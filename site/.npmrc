# npm 官方源在这台机器上可达但很慢（单次请求实测约 20 秒），用国内镜像。
# 镜像与官方源本次交叉比对返回值一致，实测 220 个包 43 秒装完。
registry=https://registry.npmmirror.com

# Tailwind v4 的 @tailwindcss/oxide 与 esbuild 都靠 optionalDependencies 分发
# 平台原生二进制（已实测 oxide-win32-x64-msvc 与 @esbuild/win32-x64 均正常加载）。
# 因此**绝不要**用 --omit=optional 安装，否则样式和构建会整体失效。
fund=false
audit=false
